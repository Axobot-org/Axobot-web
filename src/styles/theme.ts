import { createTheme } from "@mui/material";

export const AxoTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#ff66b3",
    },
    secondary: {
      main: "#26d2ea",
    },
  },
});

